const fs = require('fs');
const path = require('path');

// Let's check if we can parse the JPEG and extract dimensions or if we can use a small script with PowerShell
console.log('Generating script for PowerShell...');
